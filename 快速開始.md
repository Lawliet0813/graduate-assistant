# 🚀 快速開始

## 步驟 1: 執行快速安裝

在終端機中執行：

```bash
cd ~/Projects/graduate-assistant
./quick-install.sh
```

這個腳本會自動安裝：
- ✅ Homebrew (如果還沒有)
- ✅ PostgreSQL 16
- ✅ Python 3.12
- ✅ ChromeDriver
- ✅ Python 處環境和依賴

---

## 步驟 2: 設定 Google OAuth

### 2.1 創建 Google Cloud 專案

1. 前往 [Google Cloud Console](https://console.cloud.google.com)
2. 點擊「選擇專案」→ 「新建專案」
3. 輸入專案名稱（例如：Graduate Assistant）

### 2.2 啟用 API

1. 在左側選單中，點擊「API 和服務」→ 「程式庫」
2. 搜尋並啟用以下 API：
   - **Google Calendar API**
   - **Gmail API**

### 2.3 創建 OAuth 憑證

1. 點擊「API 和服務」→ 「憑證」
2. 點擊「建立憑證」→ 「OAuth 用戶端 ID」
3. 應用程式類型：「Web 應用程式」
4. 授權的重新導向 URI：
   ```
   http://localhost:3000/api/auth/callback/google
   ```
5. 建立後，複製「用戶端 ID」和「用戶端密碼」

### 2.4 編輯 .env 檔案

```bash
# 使用任何文字編輯器打開
open -a "Visual Studio Code" ~/Projects/graduate-assistant/.env
```

填入你的憑證：
```env
GOOGLE_CLIENT_ID="你的-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="你的-client-secret"
```

---

## 步驟 3: 啟動系統

```bash
cd ~/Projects/graduate-assistant
./start.sh
```

等待啟動完成後，打開瀏覽器前往：

**http://localhost:3000**

---

## 💡 其他指令

```bash
# 檢查服務狀態
./status.sh

# 停止所有服務
./stop.sh
```

---

## 📚 詳細文檔

完整的安裝與使用說明，請查看：

**[SETUP_GUIDE.md](./SETUP_GUIDE.md)**

---

## ❓ 常見問題

### Q: 啟動失敗？

```bash
# 1. 檢查狀態
./status.sh

# 2. 查看日誌
cat /Volumes/TEAM\ PD20M/graduate-assistant-data/postgresql/logfile

# 3. 重新啟動
./stop.sh
./start.sh
```

### Q: 登入失敗？

確保：
1. Google OAuth 憑證已正確設定
2. `.env` 檔案中的 API Keys 正確
3. Calendar 和 Gmail API 已啟用

---

## 🎉 完成!

現在你可以開始使用研究生助理系統了! 🎓